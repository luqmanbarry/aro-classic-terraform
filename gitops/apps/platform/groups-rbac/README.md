# Groups RBAC

Applies approved cluster role bindings for groups.

Safe defaults:

- no RBAC assignments are created by default
- admins must add explicit group-to-role mappings in the cluster values file

Use this chart only for reviewed RBAC changes. Do not use it as a scratch area for temporary access.
