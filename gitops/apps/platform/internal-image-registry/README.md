# Internal Image Registry

Configures the OpenShift internal image registry.

Safe defaults:

- the default public route is off
- redirect disable remains on to match common ARO registry access patterns

Review storage and route exposure before enabling this chart in production.
