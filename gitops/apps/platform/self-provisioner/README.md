# Self Provisioner

Controls whether the OpenShift self-provisioner role stays enabled.

Safe defaults:

- self-provisioning is disabled by default
- the admin contact email uses a neutral placeholder value

Most enterprise ARO environments should keep self-provisioning disabled and use approved onboarding flows instead.
